import org.springframework.data.jpa.repository.JpaRepository;

public interface ContactoRepository extends JpaRepository<Contacto, Long, String> {
    // Métodos adicionales personalizados si los necesitas
}
